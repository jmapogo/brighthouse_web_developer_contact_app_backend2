#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../cluster-service/bin/cservice" "$@"
  ret=$?
else 
  node  "$basedir/../cluster-service/bin/cservice" "$@"
  ret=$?
fi
exit $ret
